import * as alt from 'alt';
import * as graphics from 'modules/graphics/main';
import * as game from 'natives';

// alt.on('consoleCommand', (command, ...args) => {
//     if(command == 'screeneffect')
//     {
//         graphics.startScreenEffect(args[0], Number.parseInt(args[1]), JSON.parse(args[2]));
//     } else if(command == 'stopeffect')
//     {
//         graphics.stopAllScreenEffects();
//     }
// });